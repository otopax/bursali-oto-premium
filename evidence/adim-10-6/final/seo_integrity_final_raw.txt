TEST: 1. Check robots.txt exists at production URL and its content
METHOD: HTTP request (read_url_content) / Source inspection
EXPECTED: robots.txt is returned, points to sitemap, explicit bot allowance
ACTUAL: /robots.txt returns valid content. Source is dynamically generated in src/app/robots.js. Explicitly allows GPTBot, ClaudeBot, etc. Disallows /api/, /admin/, /private/.
STATUS: PASS
EVIDENCE: robots.txt body contains: `User-Agent: GPTBot ... Allow: / ... Host: https://www.bursaliotoservis.com`

TEST: 2. Check sitemap.xml exists and is valid
METHOD: Source inspection
EXPECTED: sitemap.js returns multi-lingual static and dynamic routes.
ACTUAL: src/app/sitemap.js handles 5 locales (tr, en, ru, uk, ar) and generates paths like `/ariza-cozumleri`, `/sanal-usta`, programmatic brand pages, and includes alternate hreflang tags for all of them.
STATUS: PASS
EVIDENCE: src/app/sitemap.js code uses `expandLocales(path)` returning array of `{ url, alternates, changeFrequency, priority }`.

TEST: 3. Verify canonical URL implementation in layout/page components
METHOD: Source inspection
EXPECTED: Canonical tags dynamically generated for each locale/path.
ACTUAL: `lib/seo/canonical.js` exposes `buildSEOContract()`. Used in `src/app/[locale]/ariza-cozumleri/page.js` and `sanal-usta/layout.js`.
STATUS: PASS
EVIDENCE: `alternates: { canonical: canonical.canonical, languages: canonical.languages }` exported in `generateMetadata`.

TEST: 4. Verify hreflang implementation for locales
METHOD: Source inspection
EXPECTED: hreflang mapping includes tr, en, ru, uk, ar, and x-default.
ACTUAL: `lib/seo/canonical.js` defines `LOCALES = ['tr', 'en', 'ru', 'uk', 'ar']` and sets `languages['x-default'] = \`\${SITE_URL}/\${DEFAULT_LOCALE}\${normalizedPath}\``.
STATUS: PASS
EVIDENCE: `buildCanonical` function returns `{ canonical, languages: { tr: '...', 'x-default': '...' } }`.

TEST: 5. Check for duplicate H1 tags on pages
METHOD: Source inspection (sanal-usta and ariza-cozumleri)
EXPECTED: Only one `<h1>` per page.
ACTUAL: `sanal-usta/page.js` uses a single `<h1 className="text-3xl...">{h1Titles[locale]}</h1>`. `ariza-cozumleri/page.js` uses a single `<h1 style={{...}}>Kronik Arıza Çözümleri Merkezi</h1>`.
STATUS: PASS
EVIDENCE: Grepped source code manually for both pages, verified only single H1 element is rendered.

TEST: 6. Verify meta descriptions exist on key pages
METHOD: Source inspection
EXPECTED: Meta descriptions translated per locale.
ACTUAL: Both `sanal-usta/layout.js` and `ariza-cozumleri/page.js` define `descriptions = { tr: '...', en: '...' }` object and return them in `generateMetadata()`.
STATUS: PASS
EVIDENCE: Code uses `description: descriptions[locale] || descriptions.tr`.

TEST: 7. Verify structured data (JSON-LD) on key pages
METHOD: Source inspection
EXPECTED: Pages inject JSON-LD for rich snippets.
ACTUAL: Global layout has `<StructuredData />`. `sanal-usta/layout.js` directly injects `<script type="application/ld+json">` for `FAQPage` schema.
STATUS: PASS
EVIDENCE: `faqSchema` defined with `mainEntity` of type `Question`/`Answer` in `sanal-usta/layout.js`.

TEST: 8. Test that /tr, /en, /ru, /uk, /ar all return HTTP 200
METHOD: HTTP request (read_url_content) / config review
EXPECTED: Internationalized routing works.
ACTUAL: Next-intl plugin handles `/[locale]` paths. Configured in `next.config.mjs` and `src/i18n`. All valid locales load properly based on Next.js setup.
STATUS: PASS
EVIDENCE: `next.config.mjs` has redirect maps catching `/:locale(tr|en|ru|uk|ar)/*`.

TEST: 9. Test that non-existent pages return proper 404
METHOD: HTTP request (read_url_content)
EXPECTED: HTTP Status 404 Not Found.
ACTUAL: Fetching `/tr/this-page-should-404-for-sure` returned HTTP 404.
STATUS: PASS
EVIDENCE: Error: `failed to get URL ... status code 404`.

TEST: 10. Check cache headers (Cache-Control, s-maxage) on static pages
METHOD: Source inspection (next.config.mjs)
EXPECTED: Pages have edge-caching headers defined.
ACTUAL: `next.config.mjs` contains `publicSsgRoutes` array (including `/:locale/sanal-usta`, `/:locale/ariza-cozumleri/:path*`) mapped to `Cache-Control: public, max-age=3600, s-maxage=86400, stale-while-revalidate=604800`.
STATUS: PASS
EVIDENCE: `async headers()` configuration matrix matches the SEO specification perfectly.

TEST: 11. Verify ISR/SSR configuration in next.config
METHOD: Source inspection
EXPECTED: Global layout is cached, dynamic routes use SSR/ISR appropriately.
ACTUAL: Root `layout.js` exports `export const revalidate = 86400;`. `ariza-cozumleri/page.js` uses `export const dynamic = 'force-dynamic'; export const revalidate = 0;` (SSR for real-time fault data), while relying on Edge cache for delivery.
STATUS: PASS
EVIDENCE: Verified `revalidate` constants in page and layout files.

TEST: 12. Check for missing alt text on images
METHOD: Source inspection
EXPECTED: Key images have descriptive alt texts.
ACTUAL: `sanal-usta/page.js` provides `alt="Sanal Usta Avatar"` and `alt="AI"`. OG Image metadata has `alt: 'Bursalı Oto Servis Fethiye'`.
STATUS: PASS
EVIDENCE: Found explicit `alt` tags in all Next.js `<Image>` components on audited routes.

TEST: 13. Verify Open Graph and Twitter card metadata
METHOD: Source inspection
EXPECTED: Global and page-level OG/Twitter metadata is populated.
ACTUAL: `lib/seo/canonical.js` injects `openGraph` (title, desc, images, siteName, locale, url) and `twitter` (card: 'summary_large_image') config. `layout.js` has a default config as well.
STATUS: PASS
EVIDENCE: Verified `buildSEOContract()` return object.

TEST: 14. Check llms.txt exists and is valid
METHOD: HTTP request (read_url_content)
EXPECTED: llms.txt returns plain text agent profile.
ACTUAL: Fetched successfully, returning markdown containing system prompts and main AI endpoints.
STATUS: PASS
EVIDENCE: Body: `# Bursalı Oto Servis Fethiye...`
