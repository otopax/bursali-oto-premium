SECURITY SCAN FINAL RAW
-----------------------

Missing Auth Checks:
- src/app/api/leads/route.js (GET, POST, PATCH)
- src/app/api/test-audit/route.js (GET)

Hardcoded Secrets:
- src/auth.js:154: secret: authSecret || "build-phase-dummy-secret-key-1234567890"

SQL Injection Risks:
- src/services/ChatService.js: $queryRawUnsafe
- src/scripts/test-rag.js: $queryRawUnsafe

XSS Risks (dangerouslySetInnerHTML with unsanitized content):
- src/app/[locale]/sanal-usta/page.js:479: dangerouslySetInnerHTML={{ __html: m.content }}

CORS / CSP:
- CSP headers are missing on API responses. (Set in middleware for non-API routes).

Tenant Isolation:
- /api/erp/workorders is correctly isolated.
- /api/feedback takes tenantId from request body instead of user session, allowing spoofing.
